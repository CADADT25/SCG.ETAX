@{
    Layout = null;
}
@if (ViewBag.showVIEW == true)
{
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header d-flex justify-content-end align-items-center">
                    @if (ViewBag.showCREATE == true)
                    {
                        <a id="btnCallModalCreateProfilePartner" onclick="callModalInsertProfilePartner();" class="pointer btn btn-icon icon-left text-white btn-primary">
                            <i class="fas fa-plus"></i> Create New
                        </a>
                    }
                    &nbsp;&nbsp;

                    @if (ViewBag.showEXPORT == true)
                    {
                        <a id="btnExportProfilePartner" onclick="ExportProfilePartner()" class="pointer btn btn-icon icon-left text-white btn-success">
                            <i class="fas fa-file-export"></i> Export (.csv)
                        </a>
                    }
                    &nbsp;&nbsp;
                    @if (ViewBag.showIMPORT == true)
                    {
                        <input type="file" id="inputFileImportPartner" accept=".xls,.xlsx,.csv" style="display:none;" />
                        <a id="btnImportPartner" onclick="ImportProfilePartner()" class="pointer btn btn-icon icon-left text-white btn-warning">
                            <i class="fas fa-file-import"></i> Import (.xlsx,.csv)
                        </a>
                    }
                </div>

                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped table-hover table-hover-animation" id="tbProfilePartnerMain">
                            <thead>
                                <tr>
                                    <th class="text-center">PK Key</th>
                                    <th class="text-center">No</th>
                                    <th>Customer Code</th>
                                    <th>Company Code</th>
                                    <th>sellOrg</th>
                                    <th>Sold To</th>
                                    <th>Ship To</th>
                                    <th>IsActive</th>
                                    <th></th>
                                </tr>
                            </thead>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
